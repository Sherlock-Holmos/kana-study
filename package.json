{
  "name": "japanese-study",
  "version": "15.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "content:report": "node scripts/content-report.mjs",
    "test": "node --test test/*.test.js",
    "smoke": "node scripts/smoke.mjs",
    "smoke:dist": "node scripts/smoke-dist.mjs",
    "smoke:http": "node scripts/http-smoke.mjs",
    "verify": "npm run content:report && npm run build && npm run check && npm test && npm run smoke && npm run smoke:dist && npm run smoke:http"
  }
}
